<Window x:Class="OCRApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:Controls="clr-namespace:OCRApp.Common.Controls"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:OCRApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Row="0" Width="150" Height="35" Margin="10" Click="OpenImage" Content="Open Image" FontFamily="Segoe UI" FontSize="16"/>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Controls:CustomImage x:Name="Image" Grid.Column="0" ImageLoaded="ImageLoaded"/>
            <Controls:OcrControl x:Name="Ocr" Grid.Column="1"/>
        </Grid>
        <TextBlock Grid.Row="2" Text="{Binding ElementName=ocr, Path=Status}"  FontFamily="Segoe UI" FontSize="16" Margin="10"/>
    </Grid>
</Window>
